/**
 * Central version constants used across provenance, manifests, and environment
 * capture. Bump `PLINIUS_VERSION` in lockstep with package.json / the CLI.
 */
export const PLINIUS_VERSION = "0.7.0";
export const CLI_VERSION = PLINIUS_VERSION;

/** Version of the reasoning normalizer (src/campaign/reasoning.ts). */
export const REASONING_NORMALIZER_VERSION = "1.0.0";
