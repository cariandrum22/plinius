# OpenRouter-hosted models
OPENROUTER_API_KEY=sk-or-v1-...

# Local vLLM (or any OpenAI-compatible service). Optional: leave unset if the
# server requires no authentication. Never commit real keys.
VLLM_API_KEY=
